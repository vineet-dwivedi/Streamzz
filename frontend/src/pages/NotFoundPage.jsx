function NotFoundPage() {
  return (
    <section className="stream-page">
      <header className="stream-hero glass-heavy">
        <div className="stream-hero-overlay" />
        <div className="stream-hero-content">
          <p className="stream-kicker">Lost Route</p>
          <h2 className="stream-title">404 - Scene Missing</h2>
          <p className="stream-copy">This page is not in the current catalog. Use the top navigation to continue browsing.</p>
        </div>
      </header>
    </section>
  );
}

export default NotFoundPage;
